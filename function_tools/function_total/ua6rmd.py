def vieta_sum_of_products(a3: int, a2: int) -> int:
    # Vieta's formulas state that for a quartic polynomial x^4 + a3*x^3 + a2*x^2 + a1*x + a0,
    # the sum of the products of the roots taken two at a time is given by a2 (the coefficient of x^2)
    return a2
