def calculate_leg_from_ratio(hypotenuse_length: float, leg_ratio_numerator: int, hypotenuse_ratio_denominator: int) -> float:
    return hypotenuse_length * (leg_ratio_numerator / hypotenuse_ratio_denominator)
