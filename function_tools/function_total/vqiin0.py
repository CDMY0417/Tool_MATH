def vieta_product_of_roots(a0: int) -> int:
    # Vieta's formulas state that for a quartic polynomial x^4 + a3*x^3 + a2*x^2 + a1*x + a0,
    # the product of the roots is given by the constant term a0,
    # considering the leading coefficient is 1 (monic polynomial).
    return a0
