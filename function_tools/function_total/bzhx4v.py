def reduce_ratio(numerator: float, denominator: float, divisor: float):
    reduced_numerator = numerator / divisor
    reduced_denominator = denominator / divisor
    return reduced_numerator, reduced_denominator
