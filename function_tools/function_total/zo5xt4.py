def multiply_fraction_by_number(numerator: int, denominator: int, number: int):
    new_numerator = numerator * number
    new_denominator = denominator * number
    return new_numerator, new_denominator
