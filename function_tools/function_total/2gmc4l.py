def adjust_fraction_by_factor(numerator: float, denominator: float, factor: float):
    adjusted_numerator = numerator * factor
    adjusted_denominator = denominator * factor
    return adjusted_numerator, adjusted_denominator
