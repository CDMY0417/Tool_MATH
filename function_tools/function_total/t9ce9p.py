def similar_triangle_side_length(known_side_length: float, original_scale_factor: float, target_scale_factor: float) -> float:
    return known_side_length * (target_scale_factor / original_scale_factor)
