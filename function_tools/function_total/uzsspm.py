def intersections_of_two_polynomials(poly1: str, poly2: str):
    intersection_poly = f'({poly1}) - ({poly2})'
    solutions = []
    # Actual implementation to solve the equation would go here,
    # for simplicity assume we can extract roots already
    return solutions
