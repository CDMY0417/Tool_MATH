def multiply_fraction(numerator: int, denominator: int, factor: int) -> tuple:
    new_numerator = numerator * factor
    new_denominator = denominator * factor
    return new_numerator, new_denominator
