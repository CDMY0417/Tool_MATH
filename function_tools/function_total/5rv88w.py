def factor_polynomial(expression: str, factors: list[str]):
    # Simplified placeholder for factoring a polynomial with known factors
    factored_form = ' * '.join(factors)
    return f'{expression} = ({factored_form})'
